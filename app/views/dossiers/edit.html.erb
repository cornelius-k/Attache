<h1>Editing Report</h1>

<%= render 'form', dossier: @dossier %>

<div class="sheets">
  sheets:
  <% @dossier.sheets.each do |sheet| %>
    <li><%= sheet.template %></li>
  <% end %>
  <%= link_to("New Sheet", new_dossier_sheet_path(@dossier)) %>
</div>
<%= console %>
<%= link_to 'Show', @dossier %> |
<%= link_to 'Back', dossiers_path %>
